import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    Card: {
        backgroundColor: '#eeeeee',
        padding: '1rem',
        borderWidth: 0.5,
        borderColor: '#d6d7da',
        borderRadius: 5,
        width: '15rem'
    }
});
