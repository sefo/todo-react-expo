import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    Card: {
        backgroundColor: '#eeeeee',
        padding: 16,
        borderWidth: 0.5,
        borderColor: '#d6d7da',
        borderRadius: 5,
        width: 300
    }
});
